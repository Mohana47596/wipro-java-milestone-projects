CREATE DATABASE IF NOT EXISTS hibernate_training; USE hibernate_training; CREATE TABLE IF NOT EXISTS PRODUCT(PRODUCT_ID INT PRIMARY KEY,PRODUCT_NAME VARCHAR(40),PRICE DECIMAL(10,2)); CREATE TABLE IF NOT EXISTS PRODUCT_SEQUENCE(next_val BIGINT NOT NULL); INSERT INTO PRODUCT_SEQUENCE SELECT 1 WHERE NOT EXISTS(SELECT 1 FROM PRODUCT_SEQUENCE);